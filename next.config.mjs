/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'mks-sistemas.nyc3.digitaloceanspaces.com',
      'placehold.co'
    ]
  }
};

export default nextConfig;
